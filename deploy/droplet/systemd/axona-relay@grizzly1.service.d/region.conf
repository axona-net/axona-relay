[Service]
# EXPLICIT, and deliberately NOT eagle. One relay per droplet stays in grizzly
# so the region is not left with zero nodes; region is a placement hint rather
# than a wall, but an empty region has nowhere to hint to.
Environment=RELAY_REGION=grizzly
