node_modules/
# the relay's persistent keypair(s) — never commit private keys
identity*.json
*.identity.json
*.json.lock
npm-debug.log*
.DS_Store
pow-results*.jsonl
backups/

# fleet runtime logs (start-fleet.sh writes here; churn — never commit)
relay-logs/

# Council gate: the override AUDIT LOG is tracked (it is the accountability
# record); the per-ref approval cache is ephemeral machine state.
.council/approved-*.json
